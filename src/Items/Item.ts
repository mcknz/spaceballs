export default class Item {
    constructor() {
    }
    GetCenter(gameX: Number, gameY: Number, width: Number, height: Number) {
        // let center = { x: Number, y: Number };
        // let x = (gameX - (width / 2));
        // return center;
    }
}